# each row contains the same number repeated, and the number of repetitions increases with the row number.

for num in range(10):

    for i in range(num):
        print(num, end=" ") # print number

    # new line after each row to display pattern correctly
    print("\n")
